## 1. Startup Integration

- [x] 1.1 Choose and implement a Windows startup integration path for per-user automatic launch.
- [x] 1.2 Add delayed startup support so the utility can begin after a post-login delay instead of immediately.

## 2. Observability And Recovery

- [x] 2.1 Add tray access to runtime logs.
- [x] 2.2 Implement an in-app keyboard-only emergency disable or quit path.
- [x] 2.3 Define and surface a keyboard-only OS-level recovery path that remains usable if the tray or mouse cannot be trusted.

## 3. Verification

- [x] 3.1 Verify the utility can launch automatically after login with the configured delay.
- [x] 3.2 Verify logs can be opened from the tray without using the console workflow.
- [x] 3.3 Verify keyboard-only recovery remains possible when normal mouse interaction is impaired.
